return {
	{ "akinsho/bufferline.nvim", enabled = false },
	--[[ dependencies = { "nvim-tree/nvim-web-devicons" },
	version = "*",
	opts = { -- require("bufferline").setup(opts)
		options = {
			mode = "tabs",
			separator_style = "thin",
		},
	},
	vim.keymap.set("n", "<silent>gb", "<cmd>BufferLinePick<cr>", { desc = "Find buffers" }), ]]
}
